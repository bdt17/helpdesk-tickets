<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Thomas IT Helpdesk" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Thomas IT Helpdesk">

    <!-- CDN STYLES -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- PRODUCTION CSS - ALL STYLES HERE -->

<style>
  /* Hide navbar on analytics dashboard */
  body.analytics-page .navbar { display: none !important; }

  /* PHARMA TRANSPORT COLORS */
  :root {
    --pharma-blue: #0984C0;
    --pharma-silver: #AAA7B0;
    --pharma-grey: #565759;
    --pharma-white: #FFFFFF;
    --pharma-light: #C0BEC6;
    --pharma-hover: #60BDD1;
  }

  /* BLACK DASHBOARD BG */
  body.analytics-page { 
    background: #000 !important; 
    color: white; 
  }

  /* CENTER LOGIN FORM */
  .login-form { max-width: 400px; margin: 0 auto; text-align: center; }
  .form-control { text-align: center !important; margin-bottom: 1rem; }
  .login-container { display: flex; justify-content: center; align-items: center; min-height: 50vh; padding: 2rem; }

  /* PHARMA NAVBAR */
  .navbar { 
    background: #000 !important; 
    border-bottom: 3px solid var(--pharma-blue) !important; 
  }
  .navbar-brand { 
    color: var(--pharma-blue) !important; 
    font-weight: 700 !important;
    font-size: 1.5rem !important;
  }

  /* PHARMA BUTTONS - SINGLE LOGIN */
    .btn-primary, .btn-success {
    background: var(--pharma-blue) !important;
    color: white !important;
    border: 2px solid var(--pharma-blue) !important;
    padding: 12px 24px !important;
    border-radius: 8px !important;
    font-weight: 600;
    transition: all 0.3s;
  }
  .btn-outline-light:hover, .btn-primary:hover {
    background: var(--pharma-hover) !important;
    border-color: var(--pharma-hover) !important;
    transform: translateY(-2px);
  }

  /* CARDS + STATS */
  .card { 
    background: rgba(255,255,255,0.95); 
    border: 1px solid var(--pharma-silver); 
    border-radius: 12px; 
  }
  .badge-high { background: var(--pharma-blue); }
  .badge-medium { background: var(--pharma-silver); color: #000; }

.stats-card {
  background: linear-gradient(135deg, var(--pharma-blue), var(--pharma-hover)) !important;
  color: white !important;
  border: none !important;
}
.card-title { color: white !important; font-weight: 700; }




  /* Responsive */
  @media (max-width: 768px) { .form-control { text-align: left !important; } }
  .main-content { min-height: 70vh; padding: 2rem; }
</style>


    <style>
      /* Hide navbar on analytics dashboard */
      body.analytics-page .navbar { display: none !important; }

      /* CENTER LOGIN FORM INPUTS */
      .login-form { max-width: 400px; margin: 0 auto; text-align: center; }
      .form-control {
        text-align: center !important;
        margin-bottom: 1rem;
      }
      .form-group { margin-bottom: 1.5rem; }
      .login-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 50vh;
        padding: 2rem;
      }

      /* Navbar + Cards + Buttons */
      .navbar { background: #212529 !important; color: white; }
      .btn-primary, .btn-success, .btn-outline-light {
        background: #0d6efd;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
      }
      .btn-outline-light:hover { background: #0d6efd !important; }
      .card {
        border: 1px solid #dee2e6;
        border-radius: 10px;
        padding: 20px;
        margin: 10px 0;
      }
      .badge-high { background: #dc3545; color: white; }
      .badge-medium { background: #ffc107; color: black; }

      /* Responsive */
      @media (max-width: 768px) {
        .form-control { text-align: left !important; }
      }

      .main-content { min-height: 70vh; padding: 2rem; }
    </style>
  </head>
  <body class="<%= controller_name %>-page">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container-fluid">
    <%= link_to "Thomas IT Helpdesk", root_path, class: "navbar-brand fw-bold" %>
    <div class="navbar-nav ms-auto">
      <% if session[:user_id].present? %>
        <span class="navbar-text me-3 text-light">Welcome, <%= User.find(session[:user_id]).email rescue 'Agent' %>!</span>
        <%= button_to "Log Out", logout_path, method: :delete, class: "btn btn-primary btn-sm" %>
      <% else %>
        <%= link_to "Log In", login_path, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</nav>

    <% if notice %>
      <div class="container mt-3">
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="container mt-3">
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      </div>
    <% end %>

    <main class="main-content">
      <%= yield %>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
