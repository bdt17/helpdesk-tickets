<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Thomas IT Helpdesk" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Thomas IT Helpdesk">
    
    <!-- CDN STYLES -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- HIDE NAVBAR ON ANALYTICS DASHBOARD -->
    <style>
      body.analytics-page .navbar { display: none !important; }
    </style>
  </head>
  <body class="<%= controller_name %>-page">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
      <div class="container">
        <%= link_to "Thomas IT Helpdesk", root_path, class: "navbar-brand fw-bold" %>
        <div class="navbar-nav ms-auto">
          <% if cookies.signed[:user_id].present? %>
            <span class="navbar-text me-3">Welcome, <%= User.find(cookies.signed[:user_id]).email rescue 'Agent' %></span>
            <%= button_to "Log Out", "/logout", method: :delete, class: "btn btn-outline-light btn-sm" %>
          <% else %>
            <%= link_to "Log In", new_user_session_path, class: "btn btn-outline-light btn-sm" %>
          <% end %>
        </div>
      </div>
    </nav>
    
    <% if notice %>
      <div class="container mt-3">
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="container mt-3">
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      </div>
    <% end %>
    
    <main class="main-content">
      <%= yield %>
    </main>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
