<!DOCTYPE html>
<html>
<head><title>Tech Helpdesk</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
<h1>🚀 Tech Helpdesk Live!</h1>
<div class="alert alert-success">Server working! Create tickets below 👇</div>

<%= form_with model: @ticket, url: tickets_path, local: true, class: 'mb-4' do |f| %>
  <div class="row">
    <div class="col-md-6">
      <%= f.label :title, 'Ticket Title', class: 'form-label' %>
      <%= f.text_field :title, class: 'form-control', required: true %>
    </div>
    <div class="col-md-6">
      <%= f.label :status, 'Status', class: 'form-label' %>
      <%= f.select :status, ['New', 'In Progress', 'Resolved'], {}, class: 'form-select' %>
    </div>
    <div class="col-12 mt-2">
      <%= f.label :description, 'Description', class: 'form-label' %>
      <%= f.text_area :description, class: 'form-control', rows: 3, required: true %>
    </div>
    <div class="col-12 mt-3">
      <%= f.submit 'Create Ticket', class: 'btn btn-success btn-lg' %>
    </div>
  </div>
<% end %>

<h3>Recent Tickets</h3>
<% if @tickets.any? %>
  <% @tickets.each do |ticket| %>
    <div class="card mb-3">
      <div class="card-body">
        <h5><%= ticket.title %> <span class="badge bg-<%= ticket.status == 'New' ? 'secondary' : 'success' %>"><%= ticket.status %></span></h5>
        <p><%= ticket.description %></p>
        <small>Created <%= time_ago_in_words(ticket.created_at) %> ago</small>
      </div>
    </div>
  <% end %>
<% else %>
  <div class="alert alert-info">No tickets yet - create one above!</div>
<% end %>
</body>
</html>
